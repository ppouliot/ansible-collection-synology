PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;

CREATE TABLE IF NOT EXISTS "Indexers" ("Id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, "Name" TEXT NOT NULL, "Implementation" TEXT NOT NULL, "Settings" TEXT, "ConfigContract" TEXT, "EnableRss" INTEGER, "EnableAutomaticSearch" INTEGER, "EnableInteractiveSearch" INTEGER NOT NULL, "Priority" INTEGER NOT NULL DEFAULT 25, "Tags" TEXT, "DownloadClientId" INTEGER NOT NULL DEFAULT 0, "SeasonSearchMaximumSingleEpisodeAge" INTEGER NOT NULL DEFAULT 0);

INSERT INTO Indexers VALUES(1,'IPTorrents','IPTorrents',replace('{
  "baseUrl": "{{ ipt_rss_url }}",
  "minimumSeeders": 1,
  "seedCriteria": {}
}','
',char(10)),'IPTorrentsSettings',1,0,0,25,'[]',0,0);
INSERT INTO Indexers VALUES(2,'Nyaa','Nyaa',replace('{
  "baseUrl": "https://www.nyaa.si/",
  "additionalParameters": "&cats=1_0&filter=1",
  "minimumSeeders": 1,
  "seedCriteria": {}
}','
',char(10)),'NyaaSettings',1,1,1,25,'[]',0,0);
INSERT INTO Indexers VALUES(3,'NZBGeeK','Newznab',replace('{
  "baseUrl": "{{ nzbgeek_api_url }}",
  "apiPath": "/api",
  "apiKey": "{{ nzbgeek_api_key }}",
  "categories": [
    5030,
    5040
  ],
  "animeCategories": []
}','
',char(10)),'NewznabSettings',1,1,1,25,'[]',0,0);
{% if jackett_api_key is defined and jackett_api_key|d('')|length > 0 %}
INSERT INTO Indexers VALUES(5,'Jackett-IPTorrents','Torznab',replace('{
  "minimumSeeders": 1,
  "seedCriteria": {},
  "baseUrl": "http://{{ ansible_default_ipv4.address }}:9117/torznab/iptorrents",
  "apiPath": "/api",
  "apiKey": "{{ jackett_api_key }}",
  "categories": [
    5030,
    5040
  ],
  "animeCategories": []
}','
',char(10)),'TorznabSettings',1,1,1,25,'[]',0,0);
{% endif %}
COMMIT;
