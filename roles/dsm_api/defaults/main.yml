---
# =============================================================================
# dsm_api role defaults
# DSM 7.x API-only bootstrap — no SSH required at start
# =============================================================================

# Auth
synology_dsm_api_dsm_url: "http://{{ inventory_hostname }}:5000"
synology_dsm_api_username: "admin"
synology_dsm_api_password: ""
synology_dsm_api_validate_certs: false

# DSM API admin account — must be the built-in admin or a user with full API privileges.
# SYNO.Core.* write APIs require this level of access regardless of group membership.
# Defaults to the same as bootstrap_username/password if not set explicitly.
synology_dsm_api_admin_username: "{{ synology_dsm_api_username }}"
synology_dsm_api_admin_password: "{{ synology_dsm_api_password }}"

# Feature flags (opt-in)
synology_dsm_api_enable_user_home: true
synology_dsm_api_user_home_location: "/volume1"
synology_dsm_api_user_home_recycle_bin: false

synology_dsm_api_enable_ssh: true
synology_dsm_api_ssh_port: 22
# SSH security level: normal (medium) or high
# high = stronger ciphers/MACs only (aes256, sha2, curve25519, no legacy)
synology_dsm_api_ssh_security_level: high

synology_dsm_api_enable_smb: true
synology_dsm_api_smb_workgroup: "WORKGROUP"
synology_dsm_api_enable_nfs: false
synology_dsm_api_enable_afp: false
synology_dsm_api_enable_ftp: false
synology_dsm_api_enable_sftp: false

synology_dsm_api_enable_notifications: false
synology_dsm_api_enable_security: false
synology_dsm_api_enable_network: false

# Users to create (list of dicts)
# - name: beavis
#   password: "changeme"
#   groups: [administrators, users]
#   email: ""
#   description: ""
synology_dsm_api_users: []

# Group membership overrides (list of dicts)
# - group: administrators
#   members: [beavis, peter]
synology_dsm_api_group_members: []

# Package feeds (list of dicts)
synology_dsm_api_package_feeds:
  - name: SynoCommunity
    feed: https://packages.synocommunity.com

# Packages: NOT handled here — DSM API blocks installation (error 103).
# Use synology_package_list in the dsm_configuration role (SSH phase).
synology_dsm_api_packages: []  # unused, kept for backwards compat

# Phase 1: Official packages installable via DSM HTTP API (install_from_server).
# These are in the default Synology package catalog — no feed required.
# Community packages (SynoCommunity etc.) CANNOT be installed this way;
# use synopkg install <local.spk> over SSH in Phase 2.
synology_dsm_api_package_list:
  - ContainerManager
  - Git
  - Virtualization

# Phase 2 (SSH): Community packages installed via synopkg install <local.spk>.
# Handled by the dsm_configuration role, NOT the DSM API.
# Listed here for reference / inventory override.
synology_dsm_api_community_packages:
  - python314
  - byobu

# Shared folders to create (list of dicts)
# - name: data
#   volume: /volume1
#   desc: "Data share"
#   permissions: []
synology_dsm_api_shares: []

# NTP servers
synology_dsm_api_ntp_servers:
  - 0.pool.ntp.org
  - 1.pool.ntp.org

# DDNS records (list of dicts)
# - hostname: nas.example.com
#   provider: Cloudflare
synology_dsm_api_ddns_records: []

# Security
synology_dsm_api_autoblock_enabled: true
synology_dsm_api_autoblock_attempts: 10
synology_dsm_api_autoblock_within: 5
