---
- name: Login to DSM API and obtain SID
  synology_dsm_api_request:
    base_url: "{{ synology_dsm_api_dsm_url }}"
    validate_certs: "{{ synology_dsm_api_validate_certs }}"
    cgi_name: auth.cgi
    api_name: SYNO.API.Auth
    api_version: "3"
    api_method: login
    request_method: POST
    api_params:
      account: "{{ synology_dsm_api_admin_username }}"
      passwd: "{{ synology_dsm_api_admin_password }}"
      session: ansible_bootstrap
      format: sid
  register: synology_dsm_api_login
  no_log: true

- name: Set SID fact
  ansible.builtin.set_fact:
    synology_dsm_api_sid: "{{ synology_dsm_api_login.json.data.sid }}"
  no_log: true
